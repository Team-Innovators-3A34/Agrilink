<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Prédiction de Rendement Agricole</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
    <div class="container mt-5">
        <h1 class="text-center">Prédiction de Rendement Agricole</h1>

        {% if prediction is defined and prediction is not null %}
            <div class="alert alert-info">
                <h3>Résultat de la Prédiction :</h3>
                <p><strong>Rendement Estimé :</strong> {{ prediction|number_format(2, '.', ' ') }} tonnes/ha</p>
            </div>
        {% else %}
            <p class="alert alert-warning">Aucune prédiction reçue.</p>
        {% endif %}

        <form action="{{ path('app_predict') }}" method="POST" class="mt-4">
            <div class="mb-3">
                <label for="Region" class="form-label">Région :</label>
                <input type="text" class="form-control" id="Region" name="Region" required>
            </div>

            <div class="mb-3">
                <label for="Soil_Type" class="form-label">Type de sol :</label>
                <input type="text" class="form-control" id="Soil_Type" name="Soil_Type" required>
            </div>

            <div class="mb-3">
                <label for="Crop" class="form-label">Culture :</label>
                <input type="text" class="form-control" id="Crop" name="Crop" required>
            </div>

            <div class="mb-3">
                <label for="Rainfall_mm" class="form-label">Précipitations (mm) :</label>
                <input type="number" class="form-control" id="Rainfall_mm" name="Rainfall_mm" required>
            </div>

            <div class="mb-3">
                <label for="Temperature_Celsius" class="form-label">Température (°C) :</label>
                <input type="number" class="form-control" id="Temperature_Celsius" name="Temperature_Celsius" required>
            </div>

            <div class="mb-3">
                <label for="Fertilizer_Used" class="form-label">Engrais utilisé :</label>
                <input type="checkbox" id="Fertilizer_Used" name="Fertilizer_Used">
            </div>

            <div class="mb-3">
                <label for="Irrigation_Used" class="form-label">Irrigation utilisée :</label>
                <input type="checkbox" id="Irrigation_Used" name="Irrigation_Used">
            </div>

            <div class="mb-3">
                <label for="Weather_Condition" class="form-label">Conditions météorologiques :</label>
                <input type="text" class="form-control" id="Weather_Condition" name="Weather_Condition" required>
            </div>

            <div class="mb-3">
                <label for="Days_to_Harvest" class="form-label">Jours avant la récolte :</label>
                <input type="number" class="form-control" id="Days_to_Harvest" name="Days_to_Harvest" required>
            </div>

            <button type="submit" class="btn btn-primary">Prédire</button>
        </form>
    </div>
</body>
</html>
