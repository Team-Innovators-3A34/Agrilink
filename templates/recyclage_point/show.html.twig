{% extends 'base.html.twig' %}

{% block title %}RecyclagePoint{% endblock %}

{% block body %}
    <h1>RecyclagePoint</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recyclage_point.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ recyclage_point.Name }}</td>
            </tr>
            <tr>
                <th>MaxCapacite</th>
                <td>{{ recyclage_point.MaxCapacite }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ recyclage_point.adresse }}</td>
            </tr>
            <tr>
                <th>Longitude</th>
                <td>{{ recyclage_point.Longitude }}</td>
            </tr>
            <tr>
                <th>Latitude</th>
                <td>{{ recyclage_point.Latitude }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_recyclage_point_index') }}">back to list</a>

    <a href="{{ path('app_recyclage_point_edit', {'id': recyclage_point.id}) }}">edit</a>

    {{ include('recyclage_point/_delete_form.html.twig') }}
{% endblock %}
